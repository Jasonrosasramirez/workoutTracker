/* -- Requiring variables -- */
const express = require("express"); 
const router = express.Router(); 


/* -- Building the new Schema -- */
const Schema; 






/* -- exporting -- */
module.export = router;